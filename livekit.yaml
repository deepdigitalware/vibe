port: 7880
bind_addresses:
  - ""
rtc:
  tcp_port: 7881
  udp_port: 7882
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true

# Keys are now handled via environment variables in docker-compose for security
# But you can also list them here:
# keys:
#   devkey: secret
